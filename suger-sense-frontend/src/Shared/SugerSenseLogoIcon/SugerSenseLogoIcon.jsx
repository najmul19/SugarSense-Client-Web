import { Link } from 'react-router-dom';

const SugerSenseLogoIcon = () => {
    return (
        <div>
            <Link className='text-xl font-bold' to="/">SugerSense</Link>
        </div>
    );
};

export default SugerSenseLogoIcon;